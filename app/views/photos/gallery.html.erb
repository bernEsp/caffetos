<div class="caffetos-frame"> 
	<ul class="photos">
		<% @photos.each do |photo|%>
			<li data-photo="<%= photo.id%>">
				<%= link_to(image_tag(photo.image.gallery), photo.image_url, class:"fancybox",  rel:"gallery1", title:"#{photo.description}" ) %>
			</li>
		<% end %>
	</ul>     
</div>